version: "2"

linters:
  default: standard
  enable:
    - misspell
    - revive
    - unconvert
    - unparam
    - copyloopvar
    - errorlint
    - nilerr
  settings:
    revive:
      rules:
        - name: exported

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
